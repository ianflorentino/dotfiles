# Install command-line tools using Homebrew
# Usage: `brew bundle Brewfile`

# Update to latest

brew "pkg-config"

brew "node"
brew "nvm"
brew "go"

tap "neovim/neovim"
brew "neovim", args: ["HEAD"]
